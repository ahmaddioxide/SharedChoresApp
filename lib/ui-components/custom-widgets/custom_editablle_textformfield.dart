import 'package:flutter/material.dart';

class EditableTextFormField extends StatelessWidget {
  const EditableTextFormField({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
